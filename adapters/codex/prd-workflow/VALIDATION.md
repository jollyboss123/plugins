# Codex Adapter Validation Checklist

- [ ] `.codex-plugin/plugin.json` is valid JSON and version matches canonical `1.0.0`.
- [ ] `skills/` contains exactly: `write-a-prd`, `prd-to-plan`, `do-work`.
- [ ] Each `SKILL.md` has canonical metadata fields.
- [ ] `do-work` does not mandate `prd-to-plan`.
- [ ] Trigger descriptions are concise and unambiguous.
